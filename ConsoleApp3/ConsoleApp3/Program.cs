using System;

class Program
{
    static void Main(string[] args)
    {
        //int[] numbers = ReverseArray.GenerateNumbers(6);
        //ReverseArray.Reverse(numbers);
        //ReverseArray.PrintNumbers(numbers);

        //for (int i = 1; i <= 10; i++)
        //{
        //   Console.Write(Fibonacci.Fib(i) + (i < 10 ? ", " : ""));
        //}

        //Shape shape1 = new Circle(5, "red");
        //Shape shape2 = new Square(10, "blue");

        //shape1.Draw();
        //shape2.Draw();

        //Student student = new Student("Alice", "Computer Science");
        //Instructor instructor = new Instructor("Bob", "Mathematics");

        //student.Display();
        //student.CalculateSalary();
        //instructor.Display();
        //instructor.CalculateSalary();

        //Ball redBall = new Ball(10, new Color(255, 0, 0));
        //Ball blueBall = new Ball(12, new Color(0, 0, 255));

        //redBall.Throw();
        //redBall.Throw();
        //redBall.Throw();

        //blueBall.Pop();
        //blueBall.Throw();

        //Console.WriteLine($"The red ball has been thrown {redBall.GetThrowCount()} times.");
        //Console.WriteLine($"The blue ball has been thrown {blueBall.GetThrowCount()} times."); 
    }
}

